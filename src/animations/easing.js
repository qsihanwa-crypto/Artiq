// Shared easing + duration vocabulary so every GSAP call in the app moves consistently.
export const EASE = {
  soft: 'power2.out',
  strong: 'power3.out',
  expo: 'expo.out',
  inOut: 'power2.inOut',
}

export const DURATION = {
  micro: 0.25,
  entrance: 0.7,
  showcase: 1.1,
}

export const STAGGER = {
  tight: 0.05,
  normal: 0.08,
  loose: 0.14,
}
